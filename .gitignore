node_modules/
npm-debug.log*
.env
.env.*
key
*.local.json

# Runtime journals — written next to the scripts unless JEV_GATES_HOME is set.
*.jsonl
!examples/*.jsonl

# OS noise
.DS_Store
Thumbs.db

# Adapter state, when JEV_GATES_HOME is not set.
.dsh-state/

# Isolated runtime acceptance scratch (scripts/jev-dsh-acceptance.mjs)
.acceptance/
.iso-dsh/
.iso-project/
