{
  "schemaVersion": 2,
  "taskId": "quickstart",
  "runId": "quickstart-1",
  "projectRoot": ".",
  "criteria": [{
    "id": "report-present",
    "text": "The example report exists and is not empty.",
    "kind": "deterministic",
    "required": true,
    "verificationScope": ["examples/demo-project/reports/test-fix.md"],
    "check": { "type": "artifact_nonempty" }
  }]
}
